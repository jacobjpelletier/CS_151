/* what is wrong with the following code fragment? Will this code compile if it is part
of an otherwise valid program

if (length = MIN_LENGTH)
  System.out.println("The length is minimal.");

should probably be length == MIN_LENGTH


